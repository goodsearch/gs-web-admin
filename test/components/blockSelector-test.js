var assert        = require('assert');
var blockSelector = require('../../src/components/blockSelector.js');

describe('a test', function() {
  it('should be a result', function() {
    assert(3, [1,2,3,4].indexOf(3));
  });
});
